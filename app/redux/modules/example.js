// Example reducer

const initialState = {
  isExample: true,
  error: '',
  exampleId: 'reducer-example',
}

export default function users (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}